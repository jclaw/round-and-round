export const START_ANGLE = Math.PI / 2;
export const ANIM_MS = 450;

export const TILT = Math.PI * 0.45; // 0 = flat, ~0.45π ≈ 81°
export const ORB_SIZE_FRONT = 62;   // size when closest
export const ORB_SIZE_BACK = 28;    // size when farthest
